# === Web Framework ===
Flask==3.1.2
gunicorn==21.2.0

# === Core Scientific Libraries ===
numpy==2.3.4  # Last stable 1.x release compatible with OpenCV & TensorFlow
opencv-python==4.11.0.86

# === Machine Learning ===
tensorflow==2.20.0  # Remove 'tensorflow' to avoid duplication/conflict

# === Utilities ===
requests==2.31.0
